---
description: "Use before claiming ENGINEERING work is done, fixed, or passing — requires running the verification command (test, build, lint, type-check) fresh and confirming its output before any success claim. Do NOT use when nothing can be executed: fact-checking a statement, confirming a document is accurate, or reconciling numbers has no command to run."
argument-hint: ""
allowed-tools: [Read, Glob, Grep, Bash]
---

Invoke the code-forge:verify skill and follow it exactly as presented to you.

The user invoked this command with: $ARGUMENTS
